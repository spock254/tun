using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GeneralTest : MonoBehaviour
{
    public InventoryInit inventory;
    public ItemInit item;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    //// Update is called once per frame
    //void Update()
    //{
    //    if (Input.GetKeyDown(KeyCode.Alpha0)) 
    //    {
    //        if (item == null)
    //        {
    //            Debug.LogWarning("item == null");
    //        }

    //        if (item.items == null) 
    //        {
    //            Debug.LogWarning("item.items == null");
    //        }

    //        item.items.Add(new Item(new ItemStats(PlayerStats.HEALTH, 0, 5, 5),
    //            "posion", 200));
    //    }    
    //}
}
